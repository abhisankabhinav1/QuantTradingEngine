#pragma once
#include <algorithm>
namespace qte::risk { struct RiskConfig { double maxPositionFraction=0.10,maxExposureFraction=1.0,maxDrawdown=1.0,stopLossFraction=1.0; }; class PositionSizer { public: static double volatilityTarget(double capital,double riskFraction,double annualVolatility,double price){return annualVolatility>0?capital*riskFraction/(annualVolatility*price):0;} }; class RiskManager { public: explicit RiskManager(RiskConfig c={}):config_(c){} bool allowsNewPosition(double equity,double proposedNotional,double peakEquity) const{return equity>0&&proposedNotional<=equity*config_.maxExposureFraction&&(peakEquity<=0||equity/peakEquity-1>-config_.maxDrawdown);} bool stopLoss(double entry,double current) const{return entry>0&&current/entry-1<=-config_.stopLossFraction;} private: RiskConfig config_; }; }
